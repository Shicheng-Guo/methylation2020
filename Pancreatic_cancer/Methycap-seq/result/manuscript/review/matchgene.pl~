#!/usr/bin/perl
use strict;
use Cwd;
chdir getcwd;

open F,"tumor_related_methylation.txt";
chomp(my @database=<F>);
close F;


open F1,"suppressionorpromotion.txt";
while(<F1>){
print $_ if @database ~~ chomp($_);
}

